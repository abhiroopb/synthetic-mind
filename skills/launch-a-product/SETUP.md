# Setup

## Prerequisites

1. **Cloudflare WARP VPN** — must be connected
2. **gdrive skill** — install with `sq agents skills add gdrive`, then authenticate: `cd ~/.claude/skills/gdrive && uv run gdrive-cli.py auth login`
3. **Slack MCP** — enable in your agent configuration
4. **Airtable MCP** — enable in your agent configuration

## Airtable Access

You need access to the Blueprint base in Airtable. If you don't have access, request it through your team lead or via The Hub.

- Base ID: `appoAMUCsm6spyACv`
- Projects Table: `tbluWMCGq36V7hMkj`
- Status Updates Table: `tblgBokQjqQd6QzAW`
