# RPI Setup

Install all RPI skills together via the bundle:

```bash
sq agents skills add --bundle=rpi
```

This installs: `rpi` (router), `rpi-research`, `rpi-plan`, `rpi-implement`, `rpi-iterate`.
