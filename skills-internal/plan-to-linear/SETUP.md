# Plan to Linear — Setup

## Prerequisites

1. **Linear skill**: Load the `linear` skill — this provides the CLI used for issue and project creation
2. **`LINEAR_API_KEY`**: Must be set in the environment. See the `linear` skill's [SETUP.md](../linear/SETUP.md) for instructions
3. **A plan to convert**: From task_list, a document, or conversation context
