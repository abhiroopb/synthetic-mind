# Setup: Owner Owl PR Review Reminder

## Prerequisites

- **`gh` CLI**: Must be installed and authenticated (`gh auth login`)
- **`jq`**: JSON processor (`brew install jq`)
- **`python3`**: Required for HTML parsing in the extraction script
